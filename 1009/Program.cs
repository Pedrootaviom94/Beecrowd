using System;

namespace _1009
{
    class Program
    {
        static void Main(string[] args)
        { 
            double a = double.Parse(Console.ReadLine());
            double b = double.Parse(Console.ReadLine());
            double c = double.Parse(Console.ReadLine());
            double d = double.Parse(Console.ReadLine());
            
            Console.WriteLine("TOTAL = R$ {0}", calculoproduto(c + b + p) );

           static double  (double a){
               return (a);
           }
           
           static double percentual (double p  ){
               double percentual = 15.0 / 100.00; // 15%
               return (p);
           }

           static double calculoproduto(double c, double b){

               return (c + b);
           }

        }
    }
}
